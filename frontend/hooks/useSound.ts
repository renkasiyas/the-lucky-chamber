// ABOUTME: Re-export useSound from SoundContext for backwards compatibility
// ABOUTME: All sound functionality is now managed by the global SoundProvider

export { useSound } from '../contexts/SoundContext'
